# near-ceo — working memory

`near-ceo`'s own running notes: cross-functional decisions made, deadlocks
resolved, and the reasoning behind them — the layer above any single
function's own log. Distinct from `content/parser-knowledge-base.md`
(research-automation sources) and any editorial log — this one is for
business/coordination judgment calls specifically.

Update it whenever a real cross-role deadlock gets resolved, a resourcing
call gets made, or a periodic whole-company review happens. Don't log
routine single-function decisions here — those belong to the function
that made them.

## Decisions log

- (seed this list as near-ceo actually resolves deadlocks or makes
  resourcing calls across sessions — empty at creation time, 2026-09-07.)

## Standing context

- Co-owns the North Star token-efficiency mandate (`BACKLOG.md` §1.0)
  alongside the chief editor and `parser`, enforcing it from the
  business/coordination side specifically.
