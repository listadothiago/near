# Workflow review — 2026-09-09

Codex (GPT-6), using PARSER, Product Trio, chief-editor, SEO and trendsetter
role lenses in one coordinator. No subagents or new content run.

## Priority and scope

RICE is relative judgment here, not audience telemetry. Session MoSCoW:

| Work | Reach / impact / confidence / effort | Decision |
| --- | --- | --- |
| P0.3 duplicate orchestration and stale execution defaults | All publishing; high / high / low–medium | Must |
| P1.22 plus requested image/events checks | Every write/review; high / high / medium | Must |
| Hotel and full-round coverage contract, P2.19 | Future rounds; high / high / medium | Must, foundational dependency |
| Public persona renames/columns, all-roster brains | Narrower launch scope; medium / mixed / high | Later; keep active backlog |
| AR UI, n8n, retrospective catalogue repair | Separate product/content work | Outside this instruction pass |

Product: consolidate the canonical route before adding gates. Tech: retain flat
files, existing metadata and manifests; fix draft exclusion in the checker.
UX: require viewing the real crop and preview; dimensions alone cannot judge the
operator's image/headline complaint. Chief editor: retain all six locales and
separate verdicts, including EDDIE TOR's independent reviewer. PARSER: changed-input
reuse with bounded repairs; no claim of measured savings. These are role decisions
performed in-session, not assertions that separate agents reviewed the work.

## Changes delivered

- Structural inventory of all 79 pre-existing skill entrypoints, plus three new
  skills. Detailed interaction review focused on backlog, ingestion, writing,
  translating, refresh, caretaking, batch, rotation, imagery, event and feed paths.
  Evidence inventory: `2026-09-09-evidence.json`. This was not a rewrite or live
  behavioral test of every persona.
- `near-editor` routes to `near-write-article` instead of maintaining a second
  publishing order. Backlog's forced-menu/forced-background-worker instructions
  now follow authorized autonomous execution and one-coordinator preference.
  Translator backfills must clear affected shared gates before committing.
- `docs/workflows/content-execution.md` defines selective loading, shared run
  research, per-piece evidence/hashes, invalidation, bounded repairs and honest
  usage reporting. Required roles and gates remain mandatory.
- `image-police` and `events-police` are discoverable, linked from rules, writing
  and maintenance entrypoints. They require actual evidence and distinct verdicts.
  Image repairs and child events still need execution on real articles.
- P1.22 is wired for every write/review: real card headline/snippet/crop inspection,
  technical image report, direct backlog findings and honest AR test availability.
  The checker accepts drafts and collections, isolated report directories, and
  rejects unknown/excluded slugs and empty/failed runs. This does not add AR UI or
  turn the dimension report into a headline simulator.
- Rotation contract plus `rotation-reorder`: preserve all cities and coverage debt,
  track writers/filters/columns/guides, require five new hotels per round/batch,
  retain researched quiet-city exceptions and lower-frequency persistent Tier 4.
  SEO must record actual demand/volume evidence. Existing run tiers and counters
  were preserved; reconciliation waits for a real round start.
- Hotel workflow: NITE-PORTER orchestration/credit, source-backed SEO/trendsetter/
  affiliate/backlink/AEO/PARSER preparation and Product Trio consultation (this
  pass supplies the initial workflow consultation). Nearby food/events use existing
  links/geography. Superseded affiliate prohibition replaced with approved-program,
  disclosure and editorial-independence rules. No outreach or enrollment authorized.
- Trendsetter and CMO brains added and wired (P3.12 subset). NITE-PORTER's skill
  explicitly disallows fictional hotel work/stays. No public bio files rewritten.
- Added frontmatter to content-rotation and near-backlog; repaired invalid YAML
  descriptions in ciclovia, departamento-de-vai-dar-merda, near-alter-ego, near-cfo,
  near-cpo, near-cto and near-war-room. Existing descriptions' meaning preserved.
- AGENTS/CLAUDE route future work to the shared contracts. Superseded handoffs and
  completed items archived; original operator feedback preserved verbatim in
  `docs/operator-workflow-feedback-2026-09-09.md` and remaining work numbered.

## Source preparation

All 27 saved Trends URLs attempted; all failed DNS in local retrieval. A web
fallback also failed. No current search-demand or locale-volume ranking claimed.
[AAN directory](https://aan.org/member-directory/),
[Portland Mercury](https://www.portlandmercury.com/) and
[East Bay Express](https://eastbayexpress.com/) were accessible. Mercury's current
food openings, arts and events are local leads, not proof of a global trend;
this scan does not justify replacing unfinished batch work.

[Google's Discover guidance](https://developers.google.com/search/docs/appearance/google-discover)
was checked: image recommendations do not guarantee inclusion, and automatically
cropping an image still requires checking that important details survive. The
new gate distinguishes Near's acceptance criteria from platform eligibility.

## Validation and practical limits

- `node --test scripts/discover-audit.test.mjs`: five regression cases passed
  (draft inspection, excluded draft, unknown slug, collection cover, empty run).
- All 82 skill frontmatters and the rules YAML parsed with the installed Node
  YAML library. The supplied Python quick validator could not import PyYAML;
  used local YAML validation instead, without installing dependencies.
- `node --check scripts/discover-audit.mjs`, `npx tsc --noEmit` and
  `git diff --check` passed.
- `npm run build -- --webpack` passed with network access after the sandboxed
  attempt could not resolve Google Fonts. No application components changed.
- Desk review: changed hero invalidates image/Discover checks; blocked calendar
  cannot become `none-found`; missing five-hotel quota leaves a shortfall; a
  maintenance-only city earns an exception, not publication credit; blocked
  demand preserves ordering; draft-only remains draft-only. These are instruction
  walkthroughs, not an end-to-end content run or headset test.
- Relative image URLs still require checking the actual rendered absolute
  `og:image`; the script does not resolve local optimizer URLs. Host 429 backoff
  remains P2.15b. No full-catalogue audit or specific hero repair was claimed.
- Manifest reports 20 published, nine candidates and one held; old 8/30 handoffs
  were stale. No new live verification of those articles in this pass.

Token telemetry: unavailable. No measured savings claimed.
